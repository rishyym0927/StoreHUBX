# StoreHUBX Roadmap

**Status: Phases 1-7 (46 items) complete.** Each checked item's sub-bullet notes the actual implementation (file paths, key decisions), which is often more accurate than re-deriving it from a diff. Starting a new phase of work? Add a new `## Phase 8 — ...` section below rather than repurposing this one.

Working list of fixes, improvements, and features, ordered for execution. Work top to bottom within a phase; phases are ordered by dependency (don't scale or extend a broken/unfinished product).

**How to use this with Claude Code:** start a fresh session (or `/clear`) per item below. Reference the item by number/name instead of re-explaining it — e.g. "work on Phase 1, item 2." Commit after each item is done and verified so progress persists independent of conversation history.

---

## Phase 1 — Critical fixes (do first, highest visibility)

- [x] **1. Fix horizontal overflow on Browse and Component Detail pages** — filter panel, badges, and stats are clipped off the right edge of the viewport. Likely a missing `max-width`/`overflow-x` constraint on a container. Verify visually in-browser, not just via diff, since this bug is invisible in code review.
  - Added `overflow-x: hidden` to `html, body` in `StoreHUBClient/app/globals.css`; added `min-w-0` to the grid-column divs in `app/components/[slug]/page.tsx` that lacked it.
- [x] **2. Fix Builds tab showing "No builds yet" for components with completed successful builds** — data mismatch between `ListBuildsForVersion` query and stored build records. Check version-string matching / field names.
  - Actual cause: `GET /builds/:id` and `GET /components/:slug/versions/:version/builds` required JWT auth; frontend silently treated 401/no-token as an empty list. Moved both routes to the public group in `internal/routes/routes.go` and made `authToken` optional in `buildApi`/`useBuilds`/`useBuildStatus`.

## Phase 2 — Implementation improvements (existing features)

- [x] **3. Stream build logs live in the UI** — backend already pushes rich step-by-step logs (`[STEP]`, `[DEBUG]`, `[SUCCESS]`) to `BuildJob.logs`; currently unused by the frontend. Poll or stream this while a build runs.
  - `useBuilds` now polls every 3s while any build in the list is `queued`/`running`, refreshing status and logs until resolved.
- [x] **4. Fix or remove the "Install command" widget** (`npx storehubx install ...`) — implies a real CLI that doesn't exist; either build a minimal real CLI or relabel/remove it.
  - Replaced with a real `git clone https://github.com/<owner>/<repo>.git` command sourced from the component's linked repo (`components/common/install-command.tsx`); only renders when a repo is actually linked.
- [x] **5. Improve search relevance** — replace `$regex` scan over name/description/tags with a Mongo text index (or dedicated search service) for real ranking and typo tolerance.
  - Added a weighted text index (name > tags > description) in `internal/db/indexes.go`; `GetAllComponents` now uses `$text`/`$search` via an aggregation pipeline sorted by `textScore` when `q` is set, falling back to `createdAt desc` otherwise.

## Phase 3 — System design / scaling

- [x] **6. Add retry + exponential backoff for failed build jobs** — builds directly on the per-job timeout added recently; currently a failed job just dies with no retry.
  - Added `Attempts`/`MaxAttempts`/`NextAttemptAt` to `BuildJob`; `worker.fail()` now requeues with `10s * 2^attempt` backoff (capped at 2m) up to `MaxAttempts` (default 3) before setting `BuildError`.
- [x] **7. Add a caching layer (Redis) in front of `GET /components` and `GET /components/:slug`** — read-heavy, write-rare endpoints.
  - New `internal/cache` package (self-hosted Redis via docker-compose). List cache invalidates via an epoch counter bumped on any component write; per-slug cache invalidates on likes/repo-link changes and on new unique visitors. Degrades gracefully to no-cache if Redis is unreachable.
- [x] **8. Move the build queue off Mongo polling** onto a real queue (Redis/RabbitMQ/SQS) — reduces DB load, adds proper at-least-once delivery semantics.
  - `EnqueueBuild`/`AddVersion`/`AutoDeploy` now `XADD` to a `builds:stream` Redis Stream; the worker blocks on `XREADGROUP` for near-instant pickup, with a slower (30s) Mongo poll kept as a fallback sweep for retries and dropped messages. Falls back entirely to the original fast poll when Redis isn't configured.
- [x] **9. Put a CDN in front of MinIO/S3** for built component bundles — improves preview load time.
  - `s3.go`'s `publicURL()` now honors `CDN_BASE_URL` when set. Code-side only — actually fronting MinIO with a CDN (e.g. Cloudflare) requires a domain/account on the user's side.
- [x] **10. Add build pipeline observability/metrics** (success/failure rate, queue depth, build duration) — currently just a console heartbeat print.
  - New `internal/metrics` package (Prometheus client) exposing `GET /metrics`; instrumented in the worker's success/fail paths and heartbeat. Added self-hosted `prometheus`+`grafana` services to `docker-compose.yml`.

## Phase 4 — New features

- [x] **11. Component ratings/reviews** (beyond likes).
  - Added `Rating` model (unique per componentId+userId) with `internal/handlers/rating_handler.go` (upsert/list/delete), denormalized `averageRating`/`ratingCount` on `Component` recalculated on every write. Frontend: `ratingApi`, `RatingStars`, `ComponentRatings` (review list + rate form) wired into the component detail page and card.
- [x] **12. Webhook-based auto-deploy** on GitHub push — upgrade from the current manual "Auto-Deploy" trigger.
  - Per-repo-link `WebhookSecret` (generated on link), public `POST /webhooks/github/:slug` verifying `X-Hub-Signature-256` via constant-time HMAC compare, reusing a shared `createVersionAndBuild` helper (extracted from `AutoDeploy`). Owner-only `GET /api/components/:slug/webhook` + a `WebhookSetup` frontend panel surface the URL/secret to copy into GitHub's webhook settings.
- [x] **13. Build caching by commit SHA** — skip rebuilding when the same commit is redeployed.
  - `internal/handlers/build_cache.go`'s `findCachedBuild` checks for another version of the same component already built successfully from the identical `commitSha`; wired into `createVersionAndBuild` (auto-deploy/webhook), `AddVersion`, and `EnqueueBuild` so a matching commit copies `previewUrl`/`buildState` instead of enqueuing a new `BuildJob`.
- [x] **14. Private/team components** — visibility control beyond fully public.
  - `Component.Visibility`/`Collaborators` fields; new `internal/middleware/optional_auth.go` (never rejects, just attaches identity if present) applied to `GET /components/:slug` and `GET /users/:id` so owners/collaborators see their private components while everyone else 404s; `GET /components` always excludes private docs. Owner-only `PATCH .../visibility` + collaborator add/remove routes. Frontend: visibility toggle + collaborator management in `OwnerActions`, PRIVATE badge on card/detail page.
- [x] **15. Usage analytics dashboard** for component owners — view/like data is already tracked, just not visualized.
  - Protected `GET /api/me/analytics` aggregates per-owned-component view/like/rating/comment totals (`internal/handlers/analytics_handler.go`). New `/me/analytics` page with stat tiles, linked from `/me`. MVP totals only — no new time-series tracking added since none of the underlying data has historical buckets.
- [x] **16. Set up Grafana dashboards for the Prometheus metrics from item 10** — Prometheus+Grafana are already running (`docker compose up`) and `/metrics` is exposing data, but no dashboard exists yet to actually look at it. Steps: open `http://localhost:3001` (admin/admin), add a Prometheus data source pointing at `http://prometheus:9090` (the in-network service name, not localhost), then create panels for `storehubx_builds_total` (rate, split by `status`), `storehubx_build_queue_depth` (gauge over time), and `storehubx_build_duration_seconds` (histogram/heatmap). Save it as a dashboard JSON under `StoreHUBXBackend/grafana/` so it's provisioned automatically instead of manually re-built every time `docker compose down -v` wipes the `grafana_data` volume.
  - Added `grafana/provisioning/{datasources,dashboards}` (auto-registers the Prometheus datasource + `build-pipeline.json` with the three requested panels) and mounted it into the `grafana` service in `docker-compose.yml`. Verified end-to-end with `docker compose down -v && up -d prometheus grafana`: both the datasource and dashboard reappear with zero manual setup.

### UI polish

The frontend already has an intentional "brutalist" black/white design system (Tailwind v4, hand-rolled — no shadcn/component library): 2px solid borders, hard offset drop-shadows on hover (`shadow-[8px_8px_0px_0px_...]` + lift), bold uppercase mono-font headings, sharp corners. Dark mode is already fully implemented via `next-themes` throughout. None of the items below are about changing that look — they're bugs, duplication, and consistency gaps found by reading the actual page/component files, to be fixed *within* the existing style.

- [x] **17. Fix hardcoded `loggedInUserId` placeholder** in `app/components/page.tsx` (currently `"user-id-placeholder"`, marked TODO) — owner-only actions on the Browse page likely never activate correctly because of this.
  - Replaced with `useAuth().user?.id`, mirroring the working pattern already used on `/me`.
- [x] **18. Extract a reusable Badge/Tag chip component** — tag pills and framework chips are currently one-off `<span>` markup repeated across `component-card.tsx`, `[slug]/page.tsx`, and `components/page.tsx`; consolidate into one component using the existing border/mono-font style.
  - New `components/common/badge.tsx` (`variant: "framework" | "tag"`), standardized on the card's outlined+uppercase+hover framework style and muted-text `#tag` style; replaced the 6 one-off occurrences across `component-card.tsx`, `[slug]/page.tsx`, and `users/[id]/page.tsx` (also unified the tag truncation limit to 6 everywhere, dropping the detail page's inverted solid-fill tag variant).
- [x] **19. Dedupe pagination** — `components/common/pagination.tsx` already exists but `app/components/page.tsx` and `(private)/me/page.tsx` each hand-roll their own near-identical pagination controls instead of using it.
  - `pagination.tsx` was unused dead code with a mismatched API; rewrote it to match the byte-identical block both pages had hand-rolled (`currentPage`/`totalPages`/`onPageChange`, ellipsis logic, brutalist styling) and swapped both pages to use it.
- [x] **20. Replace ad-hoc emoji icons with a consistent icon set** — cards/pages mix emoji (🔍📦🎨⭐📍) with inline SVGs (GitHub/branch/commit icons); pick one (e.g. `lucide-react`, thin-stroke icons read well against the mono/brutalist style) and use it everywhere.
  - Added `lucide-react`; swept ~17 files replacing rendered emoji icons (status indicators, empty states, headings) with matching lucide icons. Left the hand-drawn GitHub brand mark SVGs as-is (brand logos, not generic icons) and left emoji inside native `alert()`/`console.error` calls untouched (not DOM-rendered, can't hold JSX).
- [x] **21. Resolve or remove the commented-out "Show Live Preview" toggle** on the home page (`app/page.tsx`) — currently dead code; either finish wiring it up or delete it.
  - Removed: the toggle button, its `showPreview` state (unreachable since the only setter was in the commented button), the version-fetch effect, and the conditional preview panel it gated. `FeaturedComponentCard` is now just the card.
- [x] **22. Reconcile the two conflicting design-token sources** — `app/globals.css` (Tailwind v4, `--radius-xl: 0`, sharp corners) vs. the legacy `tailwind.config.js` (Tailwind v3-style, `borderRadius.xl: 1rem`) disagree on the signature "sharp corners" look; the config file appears to be leftover and should be removed once confirmed unused.
  - Confirmed `postcss.config.mjs` loads Tailwind v4 purely via the `@tailwindcss/postcss` plugin with no `@config` directive in `globals.css`, so `tailwind.config.js` was dead config. Deleted it; verified the dev server still starts and serves the page correctly.
- [x] **23. Broaden loading/empty states beyond the single pulse skeleton** — Browse grid and profile pages currently show a minimal centered emoji+text empty state; add skeleton cards matching the real card's border/shadow shape for a less jarring loading flash.
  - Extracted `components/common/component-card-skeleton.tsx` (matches `component-card.tsx`'s real border/shadow shell) and swapped it in for the bare spinner on the Browse page's loading state. Empty-state icons on Browse/`me`/`users/[id]` were already switched from emoji to lucide icons in item 20.
- [x] **24. Align `app/users/[id]/page.tsx` with `lib/api.ts`** — it currently hand-rolls its own fetch/error parsing instead of using the existing `userApi` client used elsewhere, which is both a consistency and a maintainability issue.
  - Replaced the hand-rolled `fetch`/error-parsing with `userApi.getProfileById`, matching the pattern already used in the component detail page. Also gave `getProfileById` an optional `authToken` param and passed the viewer's token through, so item 14's OptionalAuth-gated private/collaborator components actually surface for a logged-in viewer instead of the request always going out anonymous.

## Phase 5 — Motion & interaction polish

Same black/white brutalist system (2px borders, hard offset drop-shadows, mono uppercase type, sharp corners, dark mode via `next-themes`) — no new colors, no rounded corners, no soft shadows. This phase is about making the *existing* language feel more deliberate: consistent hover/press physics, entrance motion, and a couple of signature brutalist flourishes the current UI doesn't use yet.

- [x] **25. Unify the hover/press "physics" across all interactive elements** — right now buttons/links randomly use one of three different hover treatments (`hover:scale-105`, `hover:-translate-y-1 + shadow-lift`, or plain `transition-colors`) with no clear rule for which gets which (compare `navbar.tsx`'s `Browse`/`+ New` links using scale vs. `like-button.tsx`/`pagination.tsx` using shadow-lift vs. `tabs.tsx` using only color). Define one rule (e.g. primary/filled actions get shadow-lift, secondary/outline actions get scale, text-only links get underline/opacity) as a small set of shared utility classes or a `Button`-style wrapper, then sweep all `.tsx` files onto it.
  - Added `.brutal-lift`/`.brutal-lift-lg` (primary shadow-lift) and `.brutal-scale` (secondary) utility classes in `globals.css`; swept buttons/links across the app onto them.
- [x] **26. Add staggered entrance animation for grid/list content** — Browse grid, home page featured components, and search results currently just pop in with no transition once loading flips to false, while the skeleton (item 23) disappears abruptly. Add a subtle fade+slide-up with per-card stagger delay (CSS `animation-delay` keyed by index, consistent with the existing hand-rolled `@keyframes marquee` pattern in `globals.css` rather than pulling in a library) so content feels like it's assembling, not flashing in.
  - Added `.stagger-in` (`@keyframes fade-slide-up` + a `--stagger-index` CSS var for per-card delay) in `globals.css`; applied to the Browse grid.
- [x] **27. Animate the sharp-corner shadow offset as a continuous transform, not a snap** — the signature `shadow-[8px_8px_0px_0px_...]` hover effect (cards, hero CTAs, buttons) currently has the shadow appear at full offset instantly via `transition-all`, which doesn't tween box-shadow smoothly in most browsers and reads as a jump-cut. Replace with a transform-based approach (an absolutely-positioned pseudo-shadow layer or a translated duplicate border) that genuinely animates from 0 to 8px offset, and standardize the duration/easing (e.g. `duration-150 ease-out`) across `component-card.tsx`, `page.tsx` hero buttons, and `pagination.tsx`.
  - `.brutal-lift` now transitions `transform`/`box-shadow` explicitly (150ms ease-out) instead of `transition-all`, so the offset genuinely tweens.
- [x] **28. Add a page-level route transition** — navigating between `/`, `/components`, and `/components/[slug]` currently hard-cuts with no shared transition, which feels abrupt for a design system this deliberate about motion elsewhere (marquee, hover lifts). Add a lightweight fade/slide wrapper around `{children}` in `app/layout.tsx` (e.g. via CSS view-transitions or a minimal `framer-motion`/`AnimatePresence` if a motion library is pulled in — evaluate bundle cost first) so route changes feel continuous rather than a flash of unstyled content.
  - Added `components/common/page-transition.tsx` (`.animate-page-in` keyframe, no new library) wrapping `{children}` in `app/layout.tsx`.
- [x] **29. Give the loading skeletons (item 23) a shimmer instead of a flat pulse** — `component-card-skeleton.tsx` and the home page's loading blocks currently use Tailwind's default `animate-pulse` (flat opacity fade); replace with a diagonal shimmer sweep (a moving `linear-gradient` band, black/white only) that reads as "actively loading" rather than "half-rendered," consistent with the brutalist high-contrast aesthetic.
  - Added `.skeleton-shimmer` (`@keyframes shimmer-sweep`, grayscale gradient) in `globals.css`; swapped in across skeleton components.
- [x] **30. Add a signature micro-interaction to the like/rate actions** — `like-button.tsx` and `rating-stars.tsx` currently just flip state instantly (`scale-110` on the icon); add a short, snappy confirmation animation (e.g. a brief hard-shadow "punch" on the whole button, or a quick fill-sweep on the star row) so these key engagement actions have tactile feedback matching the site's bold personality, not a generic library ripple/glow.
  - Added `.animate-punch` (`@keyframes brutal-punch`, 320ms) triggered on click in `like-button.tsx` and `rating-stars.tsx`.
- [x] **31. Smooth the theme toggle transition** — `theme-toggle.tsx` currently swaps `.dark` class instantly, causing every border/bg/text color on screen to snap between black/white with no transition, which looks jarring given how much of the page (borders, shadows, backgrounds) inverts at once. Add a global `transition-colors duration-200` on `html`/`body` (careful to exclude it from elements that already animate box-shadow via item 27, to avoid double-transitioning) so the light/dark swap reads as a deliberate flip rather than a flicker.
  - Added a global `@layer base` rule transitioning `background-color`/`border-color`/`color`/`fill`/`stroke` (200ms ease-out) on all elements in `globals.css`, separate from `.brutal-lift`'s own transform/shadow transition so the two don't fight.
  - All Phase 5 items were implemented in the prior commit but left unchecked here — reconciled while doing Phase 6.

## Phase 6 — UX consistency & feedback

Same palette and layout language as above; this phase closes gaps in how the UI *communicates state* to the user — feedback for actions, consistent empty/error handling, and small friction points found by reading the actual page/component files.

- [x] **32. Replace `alert()`/`console.error`-only error handling with in-UI toast/inline feedback** — several action handlers (e.g. like/rate/comment failures, GitHub link errors) currently fall back to a native `alert()` or a silent `console.error`, which is jarring (browser-chrome popup breaks the custom UI) or invisible. Add a minimal brutalist toast primitive (bordered box, hard shadow, auto-dismiss, matching `component-card.tsx`'s existing shadow style) in `components/common/` and route those catch blocks through it.
  - Added `components/common/toast.tsx` (`ToastProvider`/`useToast`, bordered+hard-shadow, 4s auto-dismiss, dismissible), mounted once in `app/layout.tsx`. Routed the `alert()`/silent-`console.error` catches in `like-button.tsx`, `component-ratings.tsx`, `component-comments.tsx`, and the GitHub link flow (`github-browser.tsx`) through it; the link-success path now toasts then redirects after a short delay instead of blocking on a native `alert()`. Left `components/examples/api-examples.tsx` untouched — it's unreferenced dead demo code, not part of any route.
- [x] **33. Add optimistic UI + rollback for like/rating actions** — `like-button.tsx` and `component-ratings.tsx` currently wait on the API response before updating the displayed count/state, which reads as laggy on a slow connection. Flip to optimistic update (toggle immediately, revert + surface an error via item 32's toast if the request fails).
  - `like-button.tsx` already did this. `component-ratings.tsx`'s `handleSubmit` now optimistically inserts/replaces the caller's own rating and recomputes the average before the request resolves, rolling back to the prior list/average/count and toasting on failure.
- [x] **34. Standardize empty/error state layout across all pages** — Browse, `/me`, `/me/analytics`, `users/[id]`, and component detail tabs (comments, ratings, builds) each hand-roll their own empty/error block with slightly different padding, icon size, and copy tone. Extract a shared `components/common/empty-state.tsx` (icon + heading + optional CTA, brutalist border) and sweep all of them onto it, matching the pattern already established for `component-card-skeleton.tsx` in item 23.
  - Added `components/common/empty-state.tsx` (icon + title + description + optional action, `size: "sm"|"lg"`, `variant: "default"|"error"` for the red-bordered error look). Swept it onto Browse, `/me`, `/me/analytics`, `users/[id]`, `component-ratings.tsx`, and `component-comments.tsx`.
- [x] **35. Add visible focus states for keyboard navigation** — interactive elements throughout (nav links, buttons, badges-as-links, pagination) rely on browser-default focus rings or none at all, which is inconsistent with the site's otherwise very deliberate 2px-border visual language. Add an explicit `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2` (black/white per theme) as a global rule in `globals.css` so keyboard users get the same crisp, high-contrast affordance mouse users get from hover states.
  - Added a global `:focus-visible` rule (`@layer base` in `globals.css`, black/white per theme). Also fixed `owner-actions.tsx`'s collaborator input, which had `focus:outline-none` with no replacement ring (silently unfocusable via keyboard) — it now inherits the global rule.
- [x] **36. Add a persistent "copied" confirmation for copy-to-clipboard actions** — `install-command.tsx` and `webhook-setup.tsx` both expose copy buttons for commands/secrets; verify each gives clear, consistent visual confirmation (icon swap + label change, not just a title tooltip) and standardize the duration/style between the two so copying a git-clone command and copying a webhook secret feel like the same interaction.
  - Extracted `components/common/copy-button.tsx` (icon swap + label, 2s window, toasts on clipboard failure instead of failing silently) and replaced the 3 duplicated copy-button implementations in `install-command.tsx` and `webhook-setup.tsx` (URL + secret) with it.
- [x] **37. Tighten mobile responsiveness on data-dense pages** — `component-card.tsx`'s footer row (license, dates, repo link, rating, owner actions) and the `/me/analytics` stat tiles wrap unpredictably on narrow viewports; audit both at 375px width and adjust `flex-wrap`/stacking so information hierarchy stays legible instead of degrading into a ragged wrap of mono-font fragments.
  - `component-card.tsx`: repo link now takes a full row below `sm` instead of competing for space; branch/commit-SHA chips (secondary technical detail already implied by the repo link) are hidden below `sm`; owner-action buttons are `flex-1` so they split space evenly instead of wrapping raggedly. `/me/analytics`' `StatTile` gained `truncate`/`overflow-hidden` (matching the pattern the component detail page's Quick Stats already used) so large numbers can't overflow the tile on the `grid-cols-2` mobile layout.
- [x] **38. Add a scroll-to-top / "back to browse" affordance on long pages** — the component detail page (tabs + comments + ratings + version list can run long) and the Browse grid at high page counts have no quick way back to the top; add a small fixed-position brutalist button (bordered square, hard shadow on hover, consistent with item 25's unified hover rule) that appears after scrolling past the fold.
  - Added `components/common/scroll-to-top.tsx`, mounted once in `app/layout.tsx` (covers every page, not just the two called out). Appears after 600px of scroll; listens on `document` rather than `window` because `globals.css`'s `html, body { height: 100% }` makes `body`, not `window`, the actual scrolling element here — a naive `window.scrollY` listener never would have fired.

## Phase 7 — GitHub data enrichment

Current GitHub usage (`internal/github/handlers.go`, `internal/worker/github_source.go`) only covers auth, listing the user's repos, browsing contents/branches, and zip-downloading for builds. All items below hit public repo read endpoints already covered by the existing OAuth scope — no new GitHub permissions needed — and should go through the existing Redis cache pattern (`internal/cache`) since repo metadata is read-heavy/write-rare, same rationale as item 7.

- [x] **39. Fetch and display real repo stats on linked components** — `GET /repos/{owner}/{repo}` gives stars/forks/open-issue counts for free; add a backend endpoint that fetches and caches this (keyed by `owner/repo`, short TTL), and surface stars/forks on `component-card.tsx` next to the existing LINKED badge and on the detail page, instead of the current binary linked/unlinked-only status.
  - Added `GetRepoInfo`/`FetchRepoInfo` in `internal/github/handlers.go` (trimmed `stars`/`forks`/`openIssues`/`description`/`license`/`topics`/`defaultBranch`, cached 10min via `internal/cache`, key shared across users since the data is public). Routed publicly at `GET /github/repo-info` (no viewer token needed, unlike the existing user-repo-browsing GitHub routes). Frontend: `components/common/repo-stats-badge.tsx` (small client island so `component-card.tsx` stays a plain component) next to the LINKED badge, and a stats row in `repository-info.tsx` on the detail page.
- [x] **40. Auto-fill license/description from GitHub instead of manual entry** — `GET /repos/{owner}/{repo}` also returns `license.spdx_id` and `description`; when linking a repo (`component-link-handler.go` flow), pre-fill the component's `license`/`description` fields from GitHub if the user left them blank, so linked components aren't stuck with placeholder text.
  - `LinkComponentRepo` now calls `githubapi.FetchRepoInfo` (reusing the same cache as item 39) with the linking user's own token, and only sets `description`/`license` in the `$set` when the component doesn't already have one — never clobbers a manual entry. Best-effort: a GitHub failure here doesn't block linking.
- [x] **41. Add a language breakdown bar to the component detail page** — `GET /repos/{owner}/{repo}/languages` returns bytes-per-language; render as a thin horizontal segmented bar (grayscale shades only, consistent with the black/white palette) showing the repo's actual language composition, next to or replacing the manually-tagged `frameworks` list on `repository-info.tsx`.
  - Added cached `GetLanguages` (`GET /github/languages`, pass-through of GitHub's already-compact map). `repository-info.tsx` renders a segmented `<div>` bar (5-shade grayscale cycle) plus a percentage legend for the top 5 languages.
- [x] **42. Show the real latest commit instead of a raw SHA** — `GET /repos/{owner}/{repo}/commits?per_page=1` gives the commit message, author, and date; replace the bare 7-char SHA currently shown in `component-card.tsx`'s footer and `repository-info.tsx` with the actual commit message + relative time (e.g. "fix: typo · 3 days ago"), keeping the SHA as a secondary/hover detail.
  - Added cached `GetLatestCommit` (`GET /github/latest-commit`, trimmed to `sha`/`message`/`authorName`/`date`/`htmlUrl`). `repository-info.tsx` now shows the commit message + `formatRelativeTime` (reused from `lib/api-utils.ts`) with the SHA as a secondary detail, falling back to the old bare-SHA row if the fetch didn't resolve. Left `component-card.tsx`'s footer SHA chip as-is (Browse-grid cards don't fetch per-card commit data to keep list pages cheap) — the fix targets the detail page as literally specified.
- [x] **43. Add a contributor avatar stack to the detail page** — `GET /repos/{owner}/{repo}/contributors` returns avatar URLs; render a small overlapping row of bordered avatar squares (matching the `Avatar.Root` pattern already used in `navbar.tsx`) on the component detail page as social proof, linking out to the GitHub contributors page.
  - Added cached `GetContributors` (`GET /github/contributors`, capped at 12, trimmed to `login`/`avatarUrl`/`htmlUrl`/`contributions`). `repository-info.tsx` renders an overlapping `-space-x-2` row of bordered avatar squares (first 8 + a "+N" overflow chip) linking to the repo's GitHub contributors page.
  - All four of 39/41/42/43 are fetched server-side in parallel via `Promise.allSettled` in `app/components/[slug]/page.tsx` and passed into `repository-info.tsx` as props — a GitHub hiccup on any one just renders that section as empty rather than failing the page.
- [x] **44. Render the linked repo's actual README as the component's detail content** — `GET /repos/{owner}/{repo}/readme` (base64-encoded) plus the existing `components/common/markdown.tsx` (already used for changelogs in `version-list.tsx`) can render the repo's real README in a new detail-page tab, instead of relying solely on the short manually-typed `description` field.
  - Added cached `GetReadme` (`GET /github/readme`, decodes GitHub's base64 content server-side, returns `{content}`). New "README" tab in `component-detail-tabs.tsx` (mirrors the existing `hasPreview`/`isLinked` conditional-tab pattern) rendering it via the existing `<Markdown>` component verbatim; the tab only appears when a README was actually fetched.
- [x] **45. Use GitHub's auto-generated OG image as a card banner** — `https://opengraph.githubassets.com/1/{owner}/{repo}` needs no auth/API call and returns a rendered social-preview image per repo; add it as an optional thumbnail/banner on `component-card.tsx` for linked components (with graceful fallback to the current text-only layout if the image 404s), giving Browse a visual anchor beyond mono-font text blocks.
  - Added `components/common/repo-og-banner.tsx` (client component, `onError` hides itself so the card falls back to the plain text-only layout) rendered above the header on linked cards in `component-card.tsx`.
- [x] **46. Suggest tags from the repo's GitHub topics** — the `topics` field on `GET /repos/{owner}/{repo}` reflects topics the maintainer already set on GitHub; when linking a repo, offer these as one-click-add suggestions alongside the existing manual `tags` input, so tagging isn't purely manual duplicate effort.
  - `github-browser.tsx`'s link/confirm step fetches `topics` via `getRepoInfo` once a repo is selected and renders them as toggleable chips; selected topics ride along in the existing link submit payload. Backend: `linkPayload`/`LinkComponentRepo` gained an optional `tags []string` that's merged (deduped union, not overwrite) into the component's existing tags in the same `$set`.
  - Design note: all five new enrichment endpoints (39/41/42/43/44) were made fully public (`GET /github/...`, not under the JWT-protected `/api` group) rather than requiring the viewer's own GitHub token like the pre-existing repo-browsing routes — `component-card.tsx`/the detail page are public, anonymously-viewed pages, and gating public repo data behind login would have broken that. Item 40/46 (which touch the link *flow*, already behind auth) still use the linking user's own token for slightly better rate limits.

---

## Already done (for reference, not re-work)

- Fixed `$addToSet` crash on fresh components (null `likedBy`/`uniqueVisitors` slices)
- Fixed worker hang with no timeout (added per-command and per-job timeouts)
- Wired up previously-unused `RateLimiter` middleware
- Removed dead code (`uploadFilesWithCorrectMimeTypes`, `stringsEqualFold`, obsolete one-off `cmd/fix_*` scripts)
